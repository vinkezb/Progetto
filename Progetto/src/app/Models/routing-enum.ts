import { RegisterComponent } from '../register/register.component';

export enum RoutingEnum{
    home = 'index',
    lista = 'lista',
    cards = 'cards',
    feedback = 'feedback',
    login = 'login',
    register ='register',


} 